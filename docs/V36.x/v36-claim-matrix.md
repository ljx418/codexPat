# V36 Claim Matrix

文档状态：active claim matrix；V36 documentation development stage；V36.0 documentation readiness passed scoped。
当前日期：2026-06-26。

## Allowed Claims

V36 文档阶段允许声明：

- V36 是风险闭环和目标体验加固阶段；
- V36 计划使用视觉金标准、Route A2 ceiling、Route B real asset boundary、泛化矩阵、人审和产品 UX evidence；
- V35 Route A2 target-experience assessment 已 scoped passed for tested named samples only；
- Route B 在 V35 中 blocked/not executed，V36 需要真实 source-bound asset 才能比较。

## Conditional Future Claims

只有对应 evidence passed 后才能声明：

- tested named/public samples risk closure passed scoped；
- Route A2 continue with scoped evidence；
- Route B recommended for next main route；
- V36 partial scoped；
- V36 blocked scoped；
- V36 failed。

## Forbidden Ready Claims

以下只能出现在 forbidden / not-ready / must-not-claim 语境：

- Petdex parity achieved；
- automatic photo-to-animation ready for arbitrary cats；
- automatic photo-to-2D ready for arbitrary cats；
- arbitrary-cat automatic generation ready；
- provider integration verified；
- low-retry provider reliability；
- 3D ready；
- production signed release ready；
- production release ready；
- Windows ready；
- cross-platform ready；
- MCP ready；
- Claude Code integration verified；
- OS-level Codex window binding ready；
- all Codex workflows verified。

## Scan Rule

任何 V36 evidence、HTML report、final report 中出现 forbidden ready claim 且不在禁止/未就绪语境中，阶段验收失败。
