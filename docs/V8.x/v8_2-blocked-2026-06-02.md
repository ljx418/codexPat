# V8.2 Real Named-provider 3D Output Smoke

status: blocked
date: 2026-06-02

## Blocked Reason

Network to api.tripo3d.ai is unreachable (100% TCP packet loss, curl timeout
after 10s). This is the same network-level restriction observed on 2026-06-02
during initial Tripo3D API exploration (session obs 3867).

## Blocker Detail

```
Host: api.tripo3d.ai (108.160.170.41)
ping: 2 packets transmitted, 0 received, 100% loss
curl https://api.tripo3d.ai/v2/openapi: Connection timed out after 10003ms
```

Browser access works normally. CLI/curl access is blocked at routing level.
Chrome headless can access the web UI but cannot pass Authorization headers
to the API endpoint.

## Prior Progress

R1 provider confirmation completed 2026-06-02:
- text_to_model task (77c745c6-3c4d-42fc-bf05-e9da4bd1abcc) succeeded with GLB URL
- image_to_model task (82478f36-4763-48a1-a181-0326208c9b13) was created and
  running at 88% progress with 8s estimated when last checked
- Correct API format confirmed: nested `file` object with `type` + `file_token`

## Required Unblock

V8.2 requires network connectivity to api.tripo3d.ai for:
1. Polling task 82478f36 status
2. Downloading the generated GLB
3. Verifying GLB renders correctly in app context

## False-green Rule Applied

Per v8_x-claim-matrix.md false-green rules:
- "Provider job success does not prove runtime rendering."
- "A single provider smoke does not prove provider integration verified."

Even if task completes server-side, V8.2 acceptance requires the GLB to be
downloaded and verified in the runtime context.

## V8.2 Allowed Claim Once Unblocked

```
V8.2 named-provider photo-to-3D output smoke passed for tested explicit-consent
local scenario.
```

## V8.3 Impact

V8.3 (GLTF normalization + action clip contract) cannot accept provider-output
GLB until V8.2 unblocks. V8.3 may proceed with offline scanner development
using fixture GLB files, but acceptance requires V8.2 real provider output.
