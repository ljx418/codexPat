# V5.12 Runtime Imported Pack Fixtures

These fixtures are local-only test assets for V5.12 runtime imported pack rendering.

- `sprite/manifest.json` references real PNG files with PNG signatures.
- `gltf/manifest.json` references the project-authored generated prototype GLB copied into this fixture folder.
- No remote URL, local absolute path, provider payload, prompt text, token, Authorization, workspace path, config path, or raw Agent/Codex/terminal/MCP/HTTP payload is included.

The GLTF fixture is a runtime smoke asset only. It does not prove 3D readiness or production-quality character art.
